{
  "name": "handstand-web",
  "version": "1.0.0",
  "description": "Handstand Tracker — Web Frontend",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "puppeteer": "^24.37.5"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
